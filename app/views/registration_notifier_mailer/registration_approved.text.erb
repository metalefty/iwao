<%= @guest_reg.organization %> <%= @guest_reg.full_name %> 様

<%= I18n.l(@guest_reg.approved_at, format: :long) %>

以下の<%= GuestReg.model_name.human %>が承認されました。

<%= @guest_reg %>
<% ssid = IwaoConfig.fetch(:wifi_ssid) %>
ご利用方法:
ネットワーク名(SSID)「<%= ssid %>」に、以下のユーザー名とパスワードで接続してください。

Username : <%= @guest_reg.username %>
Password : <%= @guest_reg.radius_password %>

ご利用期限は <%= I18n.l(@guest_reg.not_after, format: :long) %> までとなります。

利用規約:
<%= IwaoConfig.fetch(:term_of_service) %>
